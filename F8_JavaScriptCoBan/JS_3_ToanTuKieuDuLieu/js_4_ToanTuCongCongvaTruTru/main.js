//* ++++++(((Toán Tử ++ / -- )))++++++*/
/* Prefix(Tiền tố) / Postfix(Hậu tố) */
    

    // ++a / --a:
    // Việc 1: Cộng 1 cho a
    // Việc 2: Trả về a sau khi được cộng 

    // a++ / a--:
    // Việc 1: Coppy biến a
    // Việc 2: Trừ 1 của a
    // Việc 3: Trả về a`Coppy`
var a = 6;
a++;
console.log(a);
