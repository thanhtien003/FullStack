/* ==========<<<<< Toán Tử Logical và câu điều kiên IF  >>>>>========== */

// && : trả về kết quả của vế phải để convert
// || :trả về kết quả của vế trái để convert
var a = 1;
var b = 2;
var result = "A" && "B" && "C" && "D" && "E";
var result = "A" || "B" || "C" || "D" || "E";
console.log("result: ", result);
// if (result) {
//   console.log("a<b");
// } else {
//   console.log("a>b");
// }
